# Template 3: Shopping Calculator
shopping_calculator = '''
# Shopping Calculator Template

item_price = float(input("Enter item price: "))
quantity = int(input("Enter quantity: "))
discount_percent = float(input("Enter discount %: "))
tax_percent = float(input("Enter tax %: "))

# TODO: Calculate subtotal ราคาเต็มเท่าไร
subtotal = item_price * quantity

# TODO: Calculate discount amount ได้ส่วนลด
discount = subtotal * (discount_percent /100)

# TODO: Calculate price after discount ราคาหลังลด
price = subtotal - discount

# TODO: Calculate tax amount ภาษีเท่าไร
tax = price * (tax_percent / 100)

# TODO: Calculate final total สรุปต้องจ่ายเท่าไร
final_total = price + tax

# TODO: Display itemized receipt ออกมาทางหน้าจอ


'''

print("Copy these templates to practice!")

item_price = float(input("Enter item price: "))
quantity = int(input("Enter quantity: "))
discount_percent = float(input("Enter discount %: "))
tax_percent = float(input("Enter tax %: "))

subtotal = item_price * quantity
discount = subtotal * (discount_percent /100)
price = subtotal - discount
tax = price * (tax_percent / 100)
final_total = price + tax

print("subtotal", subtotal)
print("discount", discount)
print("price", price)
print("tax", tax)
print("final_total", final_total)