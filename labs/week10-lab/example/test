#1. รับค่า text จากผู้ใช้
#2. รับค่าอัคราระที่ต้องการค้นหาจากชชผู้ใช้
#3. แสดงผลจำนวนของอัคราระในข้อความ text

#ตัวอย่างหน้าเจอ
#Insert your text: Boonchoo jitnupong
#Character to find:o
#5 letter 'o' found in 'Boonchoo jitnupong'

text = input("Insert your text:")
char = input("Character to find:")
for letter in text:
    if letter == char:
        count += 1
print(f"{count} letter '{char}' fount in {text}")

#เขียนโปรมแกรมตรวจสอบความแข็งแรง password
#นิยาม strong password คือ ยาว 8 ตัว ,อักขระ@ 1, มีตัวเลข ,มีจัวอักษร
#ตัวอย่างหน้าเจอ
#insert your password : 
#your password is not strong

password = input("Insert your password:")
lenght = len(password)
words = passwoed.split('@')
if len(word)> 1:
    left =  word[0].isalnum()
    right = word[1].isalnum()
elif:
    left = False;
    right = False;
if lenght >= 8 and len(word)== 2 and left and right:
    print("strong")
else:
    print("not strong")



print(left,right)
